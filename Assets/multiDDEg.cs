using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class multiDDEg : MonoBehaviour
{
    public List<string> data;
    public MultipleSelectionDropDown msdd;
    // Start is called before the first frame update
    void Start()
    {
        //msdd.InstantiateItem(data,true, 2);
        //msdd.InstatiateSelectedDeckList();
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
